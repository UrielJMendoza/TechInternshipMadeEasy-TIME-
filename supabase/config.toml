# Keep the checked-in Edge entrypoint aligned with the deployed ingestion
# function. The endpoint authenticates an opaque Vault-backed bearer token in
# the handler, before it imports adapters or performs network work.
project_id = "TechInternshipMadeEasy-TIME"

[db]
major_version = 17

[functions.ingest]
verify_jwt = false
entrypoint = "./functions/ingest/index.ts"
